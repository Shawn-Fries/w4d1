class Game
    def initialize(board, display, players, current_player)
        @board = board
        @display = display
        @players = players
        @current_player = current_player
    end
end